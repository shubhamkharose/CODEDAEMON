#include <stdio.h>
int main(){
       //Enter your code here.
    int sum,count,i;
    sum=0;
  scanf("%d",&i);
   for(count=1;count<=i;count++)
   { sum=sum+count;
       
   }
   printf("%d",sum);
return 0;
    
}